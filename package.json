{
  "name": "crop-image",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run wasm:build && npm run build:web",
    "build:web": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "wasm:build": "wasm-pack build crates/crop-image-wasm --target web --out-dir ../../src/wasm/pkg --release",
    "model:fetch": "node scripts/fetch-model.mjs",
    "setup": "npm run model:fetch && npm run wasm:build"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "onnxruntime-web": "^1.22.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.0"
  }
}
